# SeekDB JS Bindings: macOS x64

Binaries supporting [Node](https://nodejs.org/) bindings to the [SeekDB C API](https://github.com/oceanbase/seekdb) for macOS x64.

See [@seekdb/js-bindings](../js-bindings) for a low-level API that uses these binaries.
