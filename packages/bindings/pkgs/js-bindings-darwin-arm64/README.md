# SeekDB JS Bindings: macOS arm64

Binaries supporting [Node](https://nodejs.org/) bindings to the [SeekDB C API](https://github.com/oceanbase/seekdb) for macOS arm64.

See [@seekdb/js-bindings](../js-bindings) for a low-level API that uses these binaries.
